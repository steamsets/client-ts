/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * dead: no remaining craft can use it. surplus: owned exceeds remaining. keep: still needed
 */
export const Bucket = {
  Dead: "dead",
  Surplus: "surplus",
  Keep: "keep",
} as const;
/**
 * dead: no remaining craft can use it. surplus: owned exceeds remaining. keep: still needed
 */
export type Bucket = OpenEnum<typeof Bucket>;

export type ExchangeClassifiedCard = {
  /**
   * Average Steam market price in cents; 0 means no data
   */
  averagePriceCents: number;
  /**
   * dead: no remaining craft can use it. surplus: owned exceeds remaining. keep: still needed
   */
  bucket: Bucket;
  /**
   * Steam class id, the stable identity of the card
   */
  classId: number;
  /**
   * Gems Steam grinds this card into; 0 means unknown
   */
  gemsGoo: number;
  /**
   * Highest standing buy order in cents; real money committed for this card
   */
  highestBuyOrderCents: number;
  /**
   * Steam economy image hash
   */
  icon: string;
  /**
   * Foil side of the set
   */
  isFoil: boolean;
  /**
   * Active sell listings; a price backed by a handful of listings is one someone can move
   */
  listed: number;
  /**
   * Steam marketable flag; the exchange rejects non-marketable deposits
   */
  marketable: boolean;
  /**
   * Median price this card actually sold at over the last 30 days, in cents; 0 means nothing sold
   */
  medianSaleCents: number;
  /**
   * Card name
   */
  name: string;
  /**
   * Copies held. 0 for a card of a held app that the account lacks — the withdraw side's rows
   */
  owned: number;
  /**
   * Current lowest Steam market ask in cents; 0 means no data, not free
   */
  priceCents: number;
  /**
   * Highest sale price over the last 4 days, in cents; 0 means nothing sold
   */
  priceMax4d: number;
  /**
   * Lowest sale price over the last 4 days, in cents; 0 means nothing sold
   */
  priceMin4d: number;
  /**
   * Copies the account's remaining crafts can still consume. Equals owned when the badge levels without a cap
   */
  remaining: number;
  /**
   * Distinct days in the last 30 that saw at least one sale
   */
  saleDays30d: number;
  /**
   * Copies sold over the last 30 days
   */
  saleVolume30d: number;
  /**
   * Sell orders on the item's order book
   */
  sellOrders: number;
  /**
   * Copies safe to give away (owned minus remaining, zero unless bucket is surplus or dead)
   */
  surplus: number;
};

/** @internal */
export const Bucket$inboundSchema: z.ZodType<Bucket, z.ZodTypeDef, unknown> =
  openEnums.inboundSchema(Bucket);

/** @internal */
export const ExchangeClassifiedCard$inboundSchema: z.ZodType<
  ExchangeClassifiedCard,
  z.ZodTypeDef,
  unknown
> = z.object({
  averagePriceCents: z.number().int(),
  bucket: Bucket$inboundSchema,
  classId: z.number().int(),
  gemsGoo: z.number().int(),
  highestBuyOrderCents: z.number().int(),
  icon: z.string(),
  isFoil: z.boolean(),
  listed: z.number().int(),
  marketable: z.boolean(),
  medianSaleCents: z.number().int(),
  name: z.string(),
  owned: z.number().int(),
  priceCents: z.number().int(),
  priceMax4d: z.number().int(),
  priceMin4d: z.number().int(),
  remaining: z.number().int(),
  saleDays30d: z.number().int(),
  saleVolume30d: z.number().int(),
  sellOrders: z.number().int(),
  surplus: z.number().int(),
});

export function exchangeClassifiedCardFromJSON(
  jsonString: string,
): SafeParseResult<ExchangeClassifiedCard, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExchangeClassifiedCard$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExchangeClassifiedCard' from JSON`,
  );
}
