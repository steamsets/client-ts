/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExchangeClassifiedCard,
  ExchangeClassifiedCard$inboundSchema,
} from "./exchangeclassifiedcard.js";

export type ExchangeClassifiedApp = {
  /**
   * The app id
   */
  appId: number;
  /**
   * The account's cards for this app
   */
  cards: Array<ExchangeClassifiedCard> | null;
  /**
   * Unix ms when the first of this app's cards was catalogued — the release-date proxy for a new-card quote lockout. 0 means unknown
   */
  cardsFirstSeenAt: number;
  /**
   * The account already crafted the foil badge
   */
  foilCrafted: boolean;
  /**
   * A foil badge exists for this app
   */
  hasFoilBadge: boolean;
  /**
   * App capsule image
   */
  image: string;
  /**
   * No badge rows are known for this app; every card is kept rather than guessed dead
   */
  missingBadgeData: boolean;
  /**
   * App name
   */
  name: string;
  /**
   * Normal badge crafts left (level cap minus current level)
   */
  normalRemaining: number;
  /**
   * The normal badge levels without a cap; its cards are never dead
   */
  normalUncapped: boolean;
};

/** @internal */
export const ExchangeClassifiedApp$inboundSchema: z.ZodType<
  ExchangeClassifiedApp,
  z.ZodTypeDef,
  unknown
> = z.object({
  appId: z.number().int(),
  cards: z.nullable(z.array(ExchangeClassifiedCard$inboundSchema)),
  cardsFirstSeenAt: z.number().int(),
  foilCrafted: z.boolean(),
  hasFoilBadge: z.boolean(),
  image: z.string(),
  missingBadgeData: z.boolean(),
  name: z.string(),
  normalRemaining: z.number().int(),
  normalUncapped: z.boolean(),
});

export function exchangeClassifiedAppFromJSON(
  jsonString: string,
): SafeParseResult<ExchangeClassifiedApp, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExchangeClassifiedApp$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExchangeClassifiedApp' from JSON`,
  );
}
