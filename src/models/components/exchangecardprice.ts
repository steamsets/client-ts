/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * What the item is. A background or emoticon is priced off gemsGoo, never off the market
 */
export const ExchangeCardPriceKind = {
  Card: "card",
  Background: "background",
  Emoticon: "emoticon",
} as const;
/**
 * What the item is. A background or emoticon is priced off gemsGoo, never off the market
 */
export type ExchangeCardPriceKind = OpenEnum<typeof ExchangeCardPriceKind>;

export type ExchangeCardPrice = {
  /**
   * The app the card belongs to
   */
  appId: number;
  /**
   * The app's image URL
   */
  appImage: string;
  /**
   * The app's name, so a caller can label an item of a game it knows nothing else about
   */
  appName: string;
  /**
   * Steam class id, the stable identity of the card
   */
  classId: number;
  /**
   * Unix ms when this card was catalogued — the release-date proxy for a new-card quote lockout. 0 means unknown
   */
  firstSeenAt: number;
  /**
   * Gems Steam grinds this card into; 0 means unknown
   */
  gemsGoo: number;
  /**
   * Highest standing buy order in cents; real money committed for this card
   */
  highestBuyOrderCents: number;
  /**
   * Steam economy image hash
   */
  icon: string;
  /**
   * Foil side of the set
   */
  isFoil: boolean;
  /**
   * What the item is. A background or emoticon is priced off gemsGoo, never off the market
   */
  kind: ExchangeCardPriceKind;
  /**
   * Active sell listings; a price backed by a handful of listings is one someone can move
   */
  listed: number;
  /**
   * Steam marketable flag; the exchange rejects non-marketable cards
   */
  marketable: boolean;
  /**
   * Median price this card actually sold at over the last 30 days, in cents; 0 means nothing sold
   */
  medianSaleCents: number;
  /**
   * Card name
   */
  name: string;
  /**
   * Current lowest Steam market ask in cents; 0 means no data, not free
   */
  priceCents: number;
  /**
   * Highest sale price over the last 4 days, in cents; 0 means nothing sold
   */
  priceMax4d: number;
  /**
   * Lowest sale price over the last 4 days, in cents; 0 means nothing sold
   */
  priceMin4d: number;
  /**
   * Distinct days in the last 30 that saw at least one sale
   */
  saleDays30d: number;
  /**
   * Copies sold over the last 30 days
   */
  saleVolume30d: number;
  /**
   * Sell orders on the item's order book
   */
  sellOrders: number;
};

/** @internal */
export const ExchangeCardPriceKind$inboundSchema: z.ZodType<
  ExchangeCardPriceKind,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(ExchangeCardPriceKind);

/** @internal */
export const ExchangeCardPrice$inboundSchema: z.ZodType<
  ExchangeCardPrice,
  z.ZodTypeDef,
  unknown
> = z.object({
  appId: z.number().int(),
  appImage: z.string(),
  appName: z.string(),
  classId: z.number().int(),
  firstSeenAt: z.number().int(),
  gemsGoo: z.number().int(),
  highestBuyOrderCents: z.number().int(),
  icon: z.string(),
  isFoil: z.boolean(),
  kind: ExchangeCardPriceKind$inboundSchema,
  listed: z.number().int(),
  marketable: z.boolean(),
  medianSaleCents: z.number().int(),
  name: z.string(),
  priceCents: z.number().int(),
  priceMax4d: z.number().int(),
  priceMin4d: z.number().int(),
  saleDays30d: z.number().int(),
  saleVolume30d: z.number().int(),
  sellOrders: z.number().int(),
});

export function exchangeCardPriceFromJSON(
  jsonString: string,
): SafeParseResult<ExchangeCardPrice, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExchangeCardPrice$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExchangeCardPrice' from JSON`,
  );
}
