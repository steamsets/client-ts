/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type TradingItem = {
  appId: number;
  appImage: string;
  appName: string;
  classId: number;
  gems: number;
  icon: string;
  id: string;
  isDefinitionOnly: boolean;
  isEvent: boolean;
  isFoil: boolean;
  isSale: boolean;
  lastSoldAt: number;
  marketable: boolean;
  name: string;
  price: number;
  type: number;
};

/** @internal */
export const TradingItem$inboundSchema: z.ZodType<
  TradingItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  appId: z.number().int(),
  appImage: z.string(),
  appName: z.string(),
  classId: z.number().int(),
  gems: z.number().int(),
  icon: z.string(),
  id: z.string(),
  isDefinitionOnly: z.boolean(),
  isEvent: z.boolean(),
  isFoil: z.boolean(),
  isSale: z.boolean(),
  lastSoldAt: z.number().int(),
  marketable: z.boolean(),
  name: z.string(),
  price: z.number().int(),
  type: z.number().int(),
});

export function tradingItemFromJSON(
  jsonString: string,
): SafeParseResult<TradingItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TradingItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TradingItem' from JSON`,
  );
}
