/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type V1AccountRefreshSessionBody = {
  /**
   * A URL to the JSON Schema for this object.
   */
  dollarSchema?: string | undefined;
  /**
   * The expiration time of the refresh token
   */
  refreshExpiresAt: Date;
  /**
   * The refresh token
   */
  refreshToken: string;
  /**
   * The expiration time of the session token
   */
  sessionExpiresAt: Date;
  /**
   * The session token
   */
  sessionToken: string;
};

/** @internal */
export const V1AccountRefreshSessionBody$inboundSchema: z.ZodType<
  V1AccountRefreshSessionBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  $schema: z.string().optional(),
  refreshExpiresAt: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  refreshToken: z.string(),
  sessionExpiresAt: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  sessionToken: z.string(),
}).transform((v) => {
  return remap$(v, {
    "$schema": "dollarSchema",
  });
});

export function v1AccountRefreshSessionBodyFromJSON(
  jsonString: string,
): SafeParseResult<V1AccountRefreshSessionBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V1AccountRefreshSessionBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V1AccountRefreshSessionBody' from JSON`,
  );
}
