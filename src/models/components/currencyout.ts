/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CurrencyOut = {
  chain: string;
  contract?: string | undefined;
  decimals: number;
  name: string;
  symbol: string;
};

/** @internal */
export const CurrencyOut$inboundSchema: z.ZodType<
  CurrencyOut,
  z.ZodTypeDef,
  unknown
> = z.object({
  chain: z.string(),
  contract: z.string().optional(),
  decimals: z.number().int(),
  name: z.string(),
  symbol: z.string(),
});

export function currencyOutFromJSON(
  jsonString: string,
): SafeParseResult<CurrencyOut, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CurrencyOut$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CurrencyOut' from JSON`,
  );
}
