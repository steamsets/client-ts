/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { HistoryValue, HistoryValue$inboundSchema } from "./historyvalue.js";

/**
 * Which visibility changed: profile, apps and friends are Steam's three privacy settings, steamsets is the site-level hidden toggle
 */
export const Kind = {
  Profile: "profile",
  Apps: "apps",
  Friends: "friends",
  Steamsets: "steamsets",
} as const;
/**
 * Which visibility changed: profile, apps and friends are Steam's three privacy settings, steamsets is the site-level hidden toggle
 */
export type Kind = OpenEnum<typeof Kind>;

export type V1AccountVisibilityUpdate = {
  /**
   * Which visibility changed: profile, apps and friends are Steam's three privacy settings, steamsets is the site-level hidden toggle
   */
  kind: Kind;
  /**
   * The time the visibility was changed
   */
  updatedAt: Date;
  visibility: HistoryValue;
};

/** @internal */
export const Kind$inboundSchema: z.ZodType<Kind, z.ZodTypeDef, unknown> =
  openEnums.inboundSchema(Kind);

/** @internal */
export const V1AccountVisibilityUpdate$inboundSchema: z.ZodType<
  V1AccountVisibilityUpdate,
  z.ZodTypeDef,
  unknown
> = z.object({
  kind: Kind$inboundSchema,
  updatedAt: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  visibility: HistoryValue$inboundSchema,
});

export function v1AccountVisibilityUpdateFromJSON(
  jsonString: string,
): SafeParseResult<V1AccountVisibilityUpdate, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V1AccountVisibilityUpdate$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V1AccountVisibilityUpdate' from JSON`,
  );
}
