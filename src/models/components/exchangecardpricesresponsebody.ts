/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  ExchangeCardPrice,
  ExchangeCardPrice$inboundSchema,
} from "./exchangecardprice.js";

export type ExchangeCardPricesResponseBody = {
  /**
   * A URL to the JSON Schema for this object.
   */
  dollarSchema?: string | undefined;
  /**
   * One entry per known item. A requested class with no catalogue row is omitted rather than returned priced at zero
   */
  cards: Array<ExchangeCardPrice> | null;
};

/** @internal */
export const ExchangeCardPricesResponseBody$inboundSchema: z.ZodType<
  ExchangeCardPricesResponseBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  $schema: z.string().optional(),
  cards: z.nullable(z.array(ExchangeCardPrice$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "$schema": "dollarSchema",
  });
});

export function exchangeCardPricesResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<ExchangeCardPricesResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExchangeCardPricesResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExchangeCardPricesResponseBody' from JSON`,
  );
}
