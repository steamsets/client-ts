/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  LeaderboardAccount,
  LeaderboardAccount$inboundSchema,
} from "./leaderboardaccount.js";

export type LeaderboardChange = {
  account?: LeaderboardAccount | null | undefined;
  accountId: number;
  latestDate: Date;
  newRank: number;
  newScore: number;
  oldRank: number;
  oldScore: number;
  rankDelta: number;
  scoreDelta: number;
};

/** @internal */
export const LeaderboardChange$inboundSchema: z.ZodType<
  LeaderboardChange,
  z.ZodTypeDef,
  unknown
> = z.object({
  account: z.nullable(LeaderboardAccount$inboundSchema).optional(),
  accountId: z.number().int(),
  latestDate: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  newRank: z.number().int(),
  newScore: z.number().int(),
  oldRank: z.number().int(),
  oldScore: z.number().int(),
  rankDelta: z.number().int(),
  scoreDelta: z.number().int(),
});

export function leaderboardChangeFromJSON(
  jsonString: string,
): SafeParseResult<LeaderboardChange, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LeaderboardChange$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LeaderboardChange' from JSON`,
  );
}
