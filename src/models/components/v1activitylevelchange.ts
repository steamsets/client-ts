/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type V1ActivityLevelChange = {
  /**
   * Level after the milestone crossing
   */
  current: number;
  /**
   * Level before the milestone crossing
   */
  previous: number;
};

/** @internal */
export const V1ActivityLevelChange$inboundSchema: z.ZodType<
  V1ActivityLevelChange,
  z.ZodTypeDef,
  unknown
> = z.object({
  current: z.number().int(),
  previous: z.number().int(),
});

export function v1ActivityLevelChangeFromJSON(
  jsonString: string,
): SafeParseResult<V1ActivityLevelChange, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V1ActivityLevelChange$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V1ActivityLevelChange' from JSON`,
  );
}
