/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SupporterStatus = {
  /**
   * Donated this month
   */
  active: boolean;
  /**
   * Total months with a donation
   */
  months: number;
};

/** @internal */
export const SupporterStatus$inboundSchema: z.ZodType<
  SupporterStatus,
  z.ZodTypeDef,
  unknown
> = z.object({
  active: z.boolean(),
  months: z.number().int(),
});

export function supporterStatusFromJSON(
  jsonString: string,
): SafeParseResult<SupporterStatus, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SupporterStatus$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SupporterStatus' from JSON`,
  );
}
