/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DonateSupporter = {
  /**
   * The account id
   */
  accountId: number;
  /**
   * The avatar hash
   */
  avatar: string;
  /**
   * The account's display name
   */
  name: string;
  /**
   * The account's vanity, null if it has none
   */
  vanity: string | null;
};

/** @internal */
export const DonateSupporter$inboundSchema: z.ZodType<
  DonateSupporter,
  z.ZodTypeDef,
  unknown
> = z.object({
  accountId: z.number().int(),
  avatar: z.string(),
  name: z.string(),
  vanity: z.nullable(z.string()),
});

export function donateSupporterFromJSON(
  jsonString: string,
): SafeParseResult<DonateSupporter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DonateSupporter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DonateSupporter' from JSON`,
  );
}
