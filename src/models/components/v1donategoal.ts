/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DonateSupporter,
  DonateSupporter$inboundSchema,
} from "./donatesupporter.js";

export type V1DonateGoal = {
  /**
   * A URL to the JSON Schema for this object.
   */
  dollarSchema?: string | undefined;
  /**
   * Donations this month with no resolvable account
   */
  anonymousCount: number;
  /**
   * Configured monthly goal in cents
   */
  goalCents: number;
  /**
   * Current calendar month, UTC
   */
  month: string;
  /**
   * Sum of donation ledger rows this month, in cents
   */
  raisedCents: number;
  /**
   * Distinct accounts plus anonymous donations this month
   */
  supporterCount: number;
  /**
   * Newest first, capped at 48. Carries no donation amounts.
   */
  supporters: Array<DonateSupporter> | null;
};

/** @internal */
export const V1DonateGoal$inboundSchema: z.ZodType<
  V1DonateGoal,
  z.ZodTypeDef,
  unknown
> = z.object({
  $schema: z.string().optional(),
  anonymousCount: z.number().int(),
  goalCents: z.number().int(),
  month: z.string(),
  raisedCents: z.number().int(),
  supporterCount: z.number().int(),
  supporters: z.nullable(z.array(DonateSupporter$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "$schema": "dollarSchema",
  });
});

export function v1DonateGoalFromJSON(
  jsonString: string,
): SafeParseResult<V1DonateGoal, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => V1DonateGoal$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V1DonateGoal' from JSON`,
  );
}
