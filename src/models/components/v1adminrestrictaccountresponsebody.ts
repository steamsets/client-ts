/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type V1AdminRestrictAccountResponseBody = {
  /**
   * A URL to the JSON Schema for this object.
   */
  dollarSchema?: string | undefined;
  /**
   * Whether the account is hidden site-wide after this call
   */
  hidden: boolean;
  /**
   * The stored restriction reason, empty when the account is not restricted
   */
  reason: string;
  /**
   * Whether a staff restriction is in force after this call
   */
  restricted: boolean;
  /**
   * How many live sessions this call killed
   */
  sessionsRevoked: number;
};

/** @internal */
export const V1AdminRestrictAccountResponseBody$inboundSchema: z.ZodType<
  V1AdminRestrictAccountResponseBody,
  z.ZodTypeDef,
  unknown
> = z.object({
  $schema: z.string().optional(),
  hidden: z.boolean(),
  reason: z.string(),
  restricted: z.boolean(),
  sessionsRevoked: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "$schema": "dollarSchema",
  });
});

export function v1AdminRestrictAccountResponseBodyFromJSON(
  jsonString: string,
): SafeParseResult<V1AdminRestrictAccountResponseBody, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      V1AdminRestrictAccountResponseBody$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'V1AdminRestrictAccountResponseBody' from JSON`,
  );
}
