/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The outcome of the verification
 */
export const Outcome = {
  Valid: "VALID",
  RateLimited: "RATE_LIMITED",
  Invalid: "INVALID",
  Expired: "EXPIRED",
  Disabled: "DISABLED",
  InsufficientPermissions: "INSUFFICIENT_PERMISSIONS",
  Forbidden: "FORBIDDEN",
  UsageExceeded: "USAGE_EXCEEDED",
} as const;
/**
 * The outcome of the verification
 */
export type Outcome = ClosedEnum<typeof Outcome>;

export type DeveloperAppAnalyticsOutcome = {
  /**
   * The number of verifications with this outcome
   */
  count: number;
  /**
   * The outcome of the verification
   */
  outcome: Outcome;
};

/** @internal */
export const Outcome$inboundSchema: z.ZodNativeEnum<typeof Outcome> = z
  .nativeEnum(Outcome);

/** @internal */
export const DeveloperAppAnalyticsOutcome$inboundSchema: z.ZodType<
  DeveloperAppAnalyticsOutcome,
  z.ZodTypeDef,
  unknown
> = z.object({
  count: z.number().int(),
  outcome: Outcome$inboundSchema,
});

export function developerAppAnalyticsOutcomeFromJSON(
  jsonString: string,
): SafeParseResult<DeveloperAppAnalyticsOutcome, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DeveloperAppAnalyticsOutcome$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DeveloperAppAnalyticsOutcome' from JSON`,
  );
}
