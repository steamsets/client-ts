/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CurrencyRow = {
  active: boolean;
  chain: string;
  coinGeckoId: string;
  contract: string;
  decimals: number;
  id: number;
  isStablecoin: boolean;
  name: string;
  symbol: string;
  treasuryAddress: string;
};

/** @internal */
export const CurrencyRow$inboundSchema: z.ZodType<
  CurrencyRow,
  z.ZodTypeDef,
  unknown
> = z.object({
  active: z.boolean(),
  chain: z.string(),
  coinGeckoId: z.string(),
  contract: z.string(),
  decimals: z.number().int(),
  id: z.number().int(),
  isStablecoin: z.boolean(),
  name: z.string(),
  symbol: z.string(),
  treasuryAddress: z.string(),
});

export function currencyRowFromJSON(
  jsonString: string,
): SafeParseResult<CurrencyRow, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CurrencyRow$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CurrencyRow' from JSON`,
  );
}
