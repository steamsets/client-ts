/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type CmsVersionItem = {
  createdAt: Date;
  createdBy: number;
  documentUid: string;
  note?: string | undefined;
  payload?: any | undefined;
  uid: string;
  versionNumber: number;
};

/** @internal */
export const CmsVersionItem$inboundSchema: z.ZodType<
  CmsVersionItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  created_by: z.number().int(),
  document_uid: z.string(),
  note: z.string().optional(),
  payload: z.any().optional(),
  uid: z.string(),
  version_number: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "created_by": "createdBy",
    "document_uid": "documentUid",
    "version_number": "versionNumber",
  });
});

export function cmsVersionItemFromJSON(
  jsonString: string,
): SafeParseResult<CmsVersionItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CmsVersionItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CmsVersionItem' from JSON`,
  );
}
